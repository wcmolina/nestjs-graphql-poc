# GraphQL configuration for IDE support
schema:
  - src/schema/**/*.graphql

# Documents (queries/mutations in your codebase)
documents:
  - src/**/*.{ts,tsx}

# Extensions
extensions:
  # Endpoint configuration (for testing in IDE)
  endpoints:
    default:
      url: http://localhost:3000/graphql
